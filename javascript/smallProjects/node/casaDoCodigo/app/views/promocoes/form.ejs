<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>
    <body>
        <form class="" action="/promocoes" method="post">
            <div class="">
                <label for="mensagem">Mensagem:</label>
                <input type="text" name="mensagem">
            </div>
            <div class="">
                <select class="" name="livro[id]">
                    <%for(var i = 0; i < lista.length; i++) {%>
                        <option value="<%=lista[i].id%>"><%=lista[i].titulo%></option>
                    <%}%>
                </select>
            </div>
            <input type="submit" value="Cadastrar Promoção">
        </form>
        <!-- Rota ja configurada automaticamente para o servidor do node -->
        <script src="/socket.io/socket.io.js"></script>
        <script>
            let socket = io() // Se comunica com o servidor com o protocolo disponivel no servidor
            socket.on('novaPromocao', function(data){                
                alert('livro em promocao'+data.livro.id)
            })
        </script>
    </body>
</html>
